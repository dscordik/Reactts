import Button from '../../../Shared/ui/Button.tsx';

const DeletePostButton = () => {
    return (
        <div>
            <Button>
                Удалить пост
            </Button>
        </div>
    );
};

export default DeletePostButton;